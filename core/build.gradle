evaluationDependsOn(":core-scala") // necessary for cross-compilation

avast {
    publish {
        artifactId = "continuity-core"
    }
}

dependencies {
    compile project(":core-scala")
    testCompile "junit:junit:4.12"
    testCompile "ch.qos.logback:logback-classic:1.1.8"
}
